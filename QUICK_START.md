# 🚀 БЫСТРЫЙ СТАРТ - PRIZMBET

## ⚡ Запуск за 3 шага

### 1️⃣ Установка зависимостей

```bash
pip install -U requests beautifulsoup4 lxml schedule
```

### 2️⃣ Запуск автообновления

```bash
python auto_update_server.py
```

**Настройки по умолчанию:**
- ⏰ Обновление каждые **5 часов**
- 📊 Первое обновление **сразу при запуске**
- 📝 Логи в `update_log.txt`

### 3️⃣ Открыть сайт

Просто откройте `index.html` в браузере или запустите локальный сервер:

```bash
# Python
python -m http.server 8000

# Или Node.js
npx http-server -p 8000
```

Откройте: **http://localhost:8000**

---

## ⚙️ Настройка интервала обновления

### Способ 1: Переменная окружения (рекомендуется)

**Windows PowerShell:**
```powershell
$env:UPDATE_INTERVAL_HOURS=3
python auto_update_server.py
```

**Linux/Mac:**
```bash
UPDATE_INTERVAL_HOURS=3 python auto_update_server.py
```

### Способ 2: Редактирование файла

Откройте `auto_update_server.py` и измените:

```python
UPDATE_INTERVAL_HOURS = 3  # Было: 5
```

---

## 📊 Google Sheets интеграция (опционально)

### Шаг 1: Создание сервисного аккаунта

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите **Google Sheets API**
4. Создайте **Service Account**
5. Скачайте ключ как `credentials.json`
6. Поместите файл в корень проекта

### Шаг 2: Настройка доступа

1. Откройте вашу Google Таблицу
2. Нажмите "Настройки доступа" → "Добавить людей"
3. Вставьте email сервисного аккаунта (из `credentials.json`)
4. Права: **Редактор**

### Шаг 3: Установка библиотек

```bash
pip install gspread google-auth
```

### Шаг 4: Настройка переменных окружения

**Windows PowerShell:**
```powershell
$env:WRITE_SHEETS="1"
$env:SPREADSHEET_ID="your_spreadsheet_id_here"
$env:SHEET_NAME="Matches"
python auto_update_server.py
```

**Linux/Mac:**
```bash
export WRITE_SHEETS="1"
export SPREADSHEET_ID="your_spreadsheet_id_here"
export SHEET_NAME="Matches"
python auto_update_server.py
```

**Где найти SPREADSHEET_ID?**
В URL таблицы: `https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit`

---

## 🌐 Деплой на Netlify

### Вариант 1: Через GitHub (рекомендуется)

1. Создайте репозиторий на GitHub
2. Загрузите все файлы проекта
3. Перейдите на [Netlify](https://netlify.com)
4. Нажмите "Add new site" → "Import from Git"
5. Выберите ваш репозиторий
6. **Build settings:**
   - Build command: *(оставить пустым)*
   - Publish directory: `/`
7. Нажмите "Deploy site"

### Вариант 2: Drag & Drop

1. Перейдите на [Netlify Drop](https://app.netlify.com/drop)
2. Перетащите папку проекта в окно браузера
3. Готово!

### Автоматическое обновление через Netlify Functions

Создайте файл `netlify/functions/update.js`:

```javascript
const { spawn } = require('child_process');

exports.handler = async function(event, context) {
    return new Promise((resolve, reject) => {
        const python = spawn('python3', ['marathon_to_sheets.py']);
        
        python.on('close', (code) => {
            resolve({
                statusCode: code === 0 ? 200 : 500,
                body: JSON.stringify({ 
                    message: code === 0 ? 'Updated successfully' : 'Update failed' 
                })
            });
        });
    });
};
```

Настройте **Scheduled Functions** в Netlify Dashboard для периодического обновления.

---

## 📱 Особенности проекта

### ✨ Система фильтрации
- **4 уровня фильтрации:** спорт, лига, популярные, поиск
- **Цветовая кодировка:** каждый спорт имеет свой цвет
- **Мгновенный поиск:** по командам, лигам и ID
- **Сохранение состояния:** фильтры работают в реальном времени

### 🎨 3D анимация
- **3000 светящихся частиц** в космическом пространстве
- **Параллакс эффект** от движения мыши
- **Волновое движение** частиц
- **Оптимизировано** для мобильных устройств

### 💰 Копирование ставок
- **Один клик** - ставка скопирована в буфер обмена
- **Формат:** `ID (Команды) - Тип - Коэффициент`
- **Вибрация** при копировании (на поддерживаемых устройствах)
- **Toast-уведомления** для подтверждения

---

## 🐛 Решение проблем

### Проблема: Парсер не находит матчи

**Причина:** Изменился HTML-код сайта Marathon

**Решение:**
1. Откройте `marathon_to_sheets.py`
2. Проверьте актуальность URL в списке `SOURCES`
3. При необходимости обновите селекторы BeautifulSoup

### Проблема: "Module not found"

**Решение:**
```bash
# Установка всех зависимостей
pip install -r requirements.txt

# Или по отдельности
pip install requests beautifulsoup4 lxml schedule gspread google-auth
```

### Проблема: 3D анимация тормозит

**Решение:** Уменьшите количество частиц в `index.html`:

```javascript
const particleCount = 1500;  // Было: 3000
```

### Проблема: Google Sheets не обновляется

**Чек-лист:**
- ✅ `credentials.json` находится в корне проекта?
- ✅ Сервисный аккаунт добавлен в таблицу с правами редактора?
- ✅ `WRITE_SHEETS="1"` в переменных окружения?
- ✅ `SPREADSHEET_ID` правильный?
- ✅ Библиотеки установлены: `pip install gspread google-auth`

---

## 📊 Структура данных

### matches.json

```json
{
  "last_update": "2026-02-15 12:00:00",
  "matches": [
    {
      "sport": "football",
      "league": "Англия. Премьер-лига",
      "id": "12345",
      "date": "15 фев",
      "time": "20:00",
      "team1": "Манчестер Сити",
      "team2": "Ливерпуль",
      "p1": "2.10",
      "x": "3.40",
      "p2": "3.50",
      "p1x": "1.30",
      "p12": "1.25",
      "px2": "1.70"
    }
  ]
}
```

### Поля:

- `sport` - вид спорта: `football`, `hockey`, `basket`, `esports`
- `league` - название лиги
- `id` - уникальный ID матча
- `date` - дата в формате "ДД ммм"
- `time` - время в формате "ЧЧ:ММ"
- `team1`, `team2` - названия команд
- `p1`, `x`, `p2` - основные коэффициенты
- `p1x`, `p12`, `px2` - двойные шансы (только для футбола)

---

## 🎯 Следующие шаги

1. ✅ Запустите автообновление
2. ✅ Откройте сайт в браузере
3. ✅ (Опционально) Настройте Google Sheets
4. ✅ (Опционально) Задеплойте на Netlify

---

## 💬 Поддержка

**Telegram канал:** https://t.me/+PMrQ9Nbzu08wYmI0

**Нашли баг?** Напишите в канал или создайте Issue

---

**Сделано с 💜 для сообщества PRIZM**

💎 **PRIZMBET** — ваш криптобукмекер
