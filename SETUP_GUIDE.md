# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ ОБНОВЛЁННОГО ПАРСЕРА

## ЧТО БЫЛО ИСПРАВЛЕНО

### ✅ 1. Добавлена валидация данных
- Проверка всех обязательных полей (команды, коэффициенты)
- Удаление матчей с пустыми командами
- Проверка коэффициентов (должны быть в диапазоне 1.01-100)
- Очистка времени из названий команд
- Детальный лог отклонённых матчей

### ✅ 2. Обработка сетевых ошибок
- Retry логика (3 попытки с экспоненциальной задержкой)
- Таймауты для всех запросов (30 секунд)
- Обработка всех типов ошибок HTTP
- Graceful degradation при падении источника

### ✅ 3. Rate Limiting
- Случайные задержки между запросами (2-5 секунд)
- Защита от блокировки IP
- Логирование задержек

### ✅ 4. Улучшенное логирование
- Детальная статистика парсинга
- Количество валидных и отклонённых матчей
- Разбивка по лигам
- Информация о размере файла

---

## 📋 УСТАНОВКА

### Шаг 1: Установите зависимости

**Windows:**
```bash
pip install -r requirements.txt
```

**Linux/Mac:**
```bash
pip3 install -r requirements.txt
```

---

## 🎯 ЗАПУСК

### Вариант A: Разовый парсинг

Запустите парсер один раз:

```bash
python marathon_to_sheets.py
```

**Ожидаемый результат:**
```
======================================================================
💎 PRIZMBET ПАРСЕР MARATHON v2.0
======================================================================
⏰ Запуск: 2026-02-15 16:45:00
📊 Источников для парсинга: 14
======================================================================

[1/14] 🔍 Парсинг: Англия. Премьер-лига
   ⏳ Задержка 3.2с перед запросом...
   ✅ Найдено матчей: 8

[2/14] 🔍 Парсинг: Испания. Ла Лига
   ⏳ Задержка 4.1с перед запросом...
   ✅ Найдено матчей: 6

...

🔄 Удаление дубликатов...
   ✅ Уникальных матчей: 156

📊 Статистика валидации:
   ✅ Валидных матчей: 156
   ❌ Отклонено: 0

📋 Матчей по лигам:
   • Англия. Премьер-лига: 45
   • Испания. Ла Лига: 28
   • КХЛ: 12
   ...

💾 Сохранено в matches.json
   📁 Размер файла: 45.23 KB

======================================================================
✅ ПАРСИНГ ЗАВЕРШЁН
======================================================================
📊 Всего матчей: 156
======================================================================
```

### Вариант B: Автообновление

Запустите автоматическое обновление каждые 5 часов:

```bash
python auto_update_server.py
```

**Изменить интервал обновления:**

Windows PowerShell:
```powershell
$env:UPDATE_INTERVAL_HOURS=3
python auto_update_server.py
```

Linux/Mac:
```bash
UPDATE_INTERVAL_HOURS=3 python auto_update_server.py
```

---

## 📊 GOOGLE SHEETS (опционально)

Если хотите автоматически записывать данные в Google Sheets:

### Шаг 1: Создайте сервисный аккаунт

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google Sheets API
4. Создайте сервисный аккаунт
5. Скачайте ключ в формате JSON как `credentials.json`
6. Положите `credentials.json` в папку с проектом

### Шаг 2: Настройте доступ

1. Создайте Google Таблицу
2. Скопируйте ID таблицы из URL:
   ```
   https://docs.google.com/spreadsheets/d/[SPREADSHEET_ID]/edit
   ```
3. Откройте доступ к таблице для сервисного аккаунта:
   - Нажмите "Поделиться"
   - Добавьте email сервисного аккаунта (из credentials.json)
   - Дайте права "Редактор"

### Шаг 3: Запустите с Google Sheets

Windows PowerShell:
```powershell
$env:WRITE_SHEETS="1"
$env:SPREADSHEET_ID="ваш_id_таблицы"
$env:SHEET_NAME="Matches"
python marathon_to_sheets.py
```

Linux/Mac:
```bash
export WRITE_SHEETS="1"
export SPREADSHEET_ID="ваш_id_таблицы"
export SHEET_NAME="Matches"
python marathon_to_sheets.py
```

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: ModuleNotFoundError

**Причина:** Не установлены зависимости

**Решение:**
```bash
pip install -r requirements.txt
```

### Проблема: Таймауты при парсинге

**Причина:** Медленное соединение или Marathon блокирует запросы

**Решение 1:** Увеличьте таймаут в `marathon_to_sheets.py`:
```python
TIMEOUT = 60  # было 30
```

**Решение 2:** Увеличьте задержки между запросами:
```python
rate_limiter = RateLimiter(min_delay=5, max_delay=10)  # было 2-5
```

### Проблема: Много отклонённых матчей

**Причина:** Парсер не может правильно извлечь данные с сайта

**Решение:** Проверьте логи и посмотрите, какие матчи отклоняются:
```bash
python marathon_to_sheets.py 2>&1 | grep "⚠️"
```

Если проблема массовая, возможно изменилась структура сайта Marathon.

### Проблема: Пустой matches.json

**Причина:** Все матчи были отклонены валидацией

**Решение:** 
1. Проверьте интернет-соединение
2. Проверьте, доступен ли marathonbet.ru
3. Запустите с подробными логами

---

## 📈 МОНИТОРИНГ

### Проверка качества данных

После парсинга проверьте `matches.json`:

```bash
# Просмотр первых 50 строк
head -n 50 matches.json

# Поиск пустых полей
grep '""' matches.json

# Подсчёт матчей
grep '"id"' matches.json | wc -l
```

### Проверка логов автообновления

```bash
tail -f update_log.txt
```

---

## 🔄 ОБНОВЛЕНИЕ ПАРСЕРА

Если вышла новая версия парсера:

```bash
# Сделайте backup текущих данных
cp matches.json matches.json.backup

# Скачайте новую версию marathon_to_sheets.py
# Замените старый файл

# Запустите тест
python marathon_to_sheets.py

# Если всё ок, запустите автообновление
python auto_update_server.py
```

---

## ✅ ЧЕКЛИСТ ПОСЛЕ УСТАНОВКИ

- [ ] Установлены все зависимости (`pip install -r requirements.txt`)
- [ ] Парсер запускается без ошибок (`python marathon_to_sheets.py`)
- [ ] Создаётся файл `matches.json`
- [ ] В `matches.json` есть данные (>50 матчей)
- [ ] Все матчи имеют команды (нет пустых `team1` и `team2`)
- [ ] Коэффициенты в нормальных пределах (1.01-100)
- [ ] Автообновление работает (`python auto_update_server.py`)
- [ ] (Опционально) Google Sheets интеграция работает

---

## 📞 ПОДДЕРЖКА

Если возникли проблемы:

1. **Проверьте логи:** `update_log.txt`
2. **Telegram канал:** https://t.me/+PMrQ9Nbzu08wYmI0
3. **Документация:** README.md в проекте

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

После успешной установки:

1. **Настройте автозапуск** (Windows Task Scheduler / Linux cron)
2. **Деплойте сайт** на Netlify или другой хостинг
3. **Протестируйте** на разных устройствах
4. **Соберите обратную связь** от пользователей

---

**Удачи! 💎🚀**
