# 📋 ПЛАН ИСПРАВЛЕНИЙ GOOGLE SHEETS

## ✅ Выполнено (Страницы БК)

### 1. winline.html
- ✅ Добавлены фильтры по видам спорта (Футбол, Хоккей, Баскетбол, Киберспорт, Теннис)
- ✅ Добавлен поиск по командам, лигам, ID
- ✅ Добавлен фильтр по лигам (динамический)
- ✅ Кнопки коэффициентов кликабельны — копируют ставку
- ✅ Кнопка "Проверить" работает — открывает Winline
- ✅ Кнопка "Поделиться" — копирует ссылку на матч
- ✅ Toast уведомления о копировании

### 2. marathon.html
- ✅ Все функции как на winline.html
- ⏳ Нет данных (нужен запуск парсера Marathon)

### 3. fonbet.html
- ✅ Все функции как на winline.html
- ⏳ Нет данных (парсер в разработке)

---

## 🔄 Google Sheets - План разделения

### Текущая структура:
```
Один лист "Matches" со всеми матчами:
sport | league | id | date | time | team1 | team2 | 1 | X | 2 | 1X | 12 | X2 | Ссылка
```

### Новая структура (План):

#### Вариант 1: Раздельные листы по БК
```
📊 PRIZMBET Матчи
├── Winline (все матчи Winline)
├── Marathon (все матчи Marathon)
├── Fonbet (все матчи Fonbet)
└── Summary (сводная статистика)
```

#### Вариант 2: Разделение по видам спорта + БК
```
📊 PRIZMBET Матчи
├── ⚽ Football
│   ├── Winline
│   ├── Marathon
│   └── Fonbet
├── 🏒 Hockey
│   ├── Winline
│   ├── Marathon
│   └── Fonbet
├── 🏀 Basketball
│   ├── Winline
│   ├── Marathon
│   └── Fonbet
├── 🎮 Esports
│   ├── Winline
│   ├── Marathon
│   └── Fonbet
└── 📈 Summary
```

#### Вариант 3: Фильтруемая таблица (Рекомендуемый)
```
📊 PRIZMBET Матчи (Единый лист с фильтрами)
├── Все данные (sport, league, bk_source, ...)
├── Фильтры:
│   ├── По БК (Winline/Marathon/Fonbet)
│   ├── По виду спорта
│   └── По лиге
└── Сводные таблицы (отдельно)
```

---

## 🎯 Рекомендация

**Вариант 3** — Единая таблица с фильтрами Google Sheets:

### Преимущества:
- ✅ Проще поддерживать (один источник данных)
- ✅ Google Sheets фильтры работают быстро
- ✅ upload_to_sheets.py не требует изменений
- ✅ Можно создать сводные таблицы (Pivot Tables)

### Реализация:
1. Добавить колонку `bk_source` (winline/marathon/fonbet)
2. Создать фильтры Google Sheets (Data → Create a filter)
3. Создать сводные таблицы по БК и видам спорта
4. Закрепить шапку (View → Freeze → 1 row)

---

## 📝 Приоритеты

### Приоритет 1: Футбол (самый популярный)
```
Фильтр: sport = 'football'
Лиги:
- Лига Чемпионов УЕФА
- Лига Европы УЕФА
- Англия. Премьер-лига
- Испания. Ла Лига
- Германия. Бундеслига
- Италия. Серия A
- Франция. Лига 1
- Россия. Премьер-лига
```

### Приоритет 2: Хоккей
```
Фильтр: sport = 'hockey'
Лиги:
- КХЛ
- НХЛ
- ВХЛ
```

### Приоритет 3: Баскетбол
```
Фильтр: sport = 'basket'
Лиги:
- NBA
- Евролига
```

### Приоритет 4: Киберспорт
```
Фильтр: sport = 'esports'
Игры:
- Dota 2
- CS2
- Valorant
```

---

## 🚀 Следующие шаги

1. **Обновить upload_to_sheets.py** — добавить колонку `bk_source`
2. **Настроить Google Sheets фильтры** — вручную или через API
3. **Создать сводные таблицы** — по БК и видам спорта
4. **Автоматическое обновление** — через GitHub Actions

---

## 📊 Скрипт для разделения (Python)

```python
# Разделение данных по листам
import gspread

gc = gspread.authorize(creds)
sheet = gc.open_by_key(SHEET_ID)

# Создаём листы
worksheets = {
    'Winline': [],
    'Marathon': [],
    'Fonbet': [],
    'Football': [],
    'Hockey': [],
    'Basket': [],
    'Esports': []
}

# Загружаем данные
all_matches = worksheet.get_all_records()

# Распределяем по листам
for row in all_matches[1:]:  # без заголовка
    bk = row.get('source', '')
    sport = row.get('sport', '')
    
    if 'winline' in bk.lower():
        worksheets['Winline'].append(row)
    if 'marathon' in bk.lower():
        worksheets['Marathon'].append(row)
    
    if sport == 'football':
        worksheets['Football'].append(row)
    elif sport == 'hockey':
        worksheets['Hockey'].append(row)
    # и т.д.

# Обновляем листы
for name, data in worksheets.items():
    if name in [w.title for w in sheet.worksheets()]:
        ws = sheet.worksheet(name)
    else:
        ws = sheet.add_worksheet(title=name, rows=1000, cols=20)
    
    # Заголовок + данные
    header = list(all_matches[0].keys())
    rows = [header] + [[row.get(h, '') for h in header] for row in data]
    ws.clear()
    ws.update('A1', rows)
```

---

**Готово к реализации!**
